{
  "name": "@spark/shared",
  "version": "0.1.0",
  "private": true,
  "main": "src/index.ts",
  "types": "src/index.ts",
  "scripts": {
    "lint": "echo \"(no lint configured for @spark/shared)\"",
    "typecheck": "tsc --noEmit",
    "build": "echo \"(types-only package)\""
  },
  "devDependencies": {
    "typescript": "^5.6.3"
  }
}
